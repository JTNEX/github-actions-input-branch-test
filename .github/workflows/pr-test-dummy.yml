name: "PR Test (Dummy on Main)"

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Feature enhanced version
        run: |
          echo "VERSION=FEATURE_ENHANCED"
          echo "This is the ENHANCED workflow from feature branch."
          echo "ref_name=${{ github.ref_name }}"
          head -n 6 .github/workflows/pr-test-dummy.yml
